{
  "name": "graft",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "bun run build.ts",
    "dev": "bun run build.ts --watch"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/chrome": "^0.1.32"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
